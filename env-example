PORT=8080
GITHUB_TOKEN1=
GITHUB_TOKEN2=
GITHUB_API=https://api.github.com/graphql
ENABLE_REDIS=
REDIS_PORT=6379
REDIS_HOST=
REDIS_USERNAME=
REDIS_PASSWORD=

# Optional: Self-hosting convenience
# If set, requests without `?username=` will use this username.
DEFAULT_USERNAME=
# If true, ignore any incoming `?username=` and always use DEFAULT_USERNAME.
FORCE_DEFAULT_USERNAME=false
